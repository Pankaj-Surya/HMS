@{
    ViewBag.Title = "Index";
}


<div class="row">
    <div class="col col-lg-12 ">
        <h2>Accomodation Types</h2>
        <hr />
    </div>
</div>
<div class="row">
    <div class="col col-lg-12 mb-3">
        <div class="input-group">
            <input type="text" class="form-control" placeholder="Search Here.">
            <button class="btn btn-outline-primary" type="button">Search</button>
            <button class="btn btn-outline-secondary" type="button">Reset</button>
            <button id="createButton" class="btn btn-outline-success" type="button" data-toggle="modal" data-target="#actionModal">Create</button>
<button  class="btn btn-outline-success" type="button" data-toggle="modal" data-target="#actionModal1">Create</button>

</div>
    </div>
</div>
<div class="row">
    <div class="col col-lg-12">
        {
            Html.RenderAction("Listing");
        }
    </div>
</div>
 Modal 
<div class="modal fade" id="actionModal1" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
        @if (Model != null)
        {
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" >Create</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">x</button>
                </div>
                <div class="modal-body">
                    <form>
                        <div class="mb-3">
                            <label>Name</label>
                            <input class="form-control" placeholder="Enter Accomodattion Type Name">

                        </div>
                        <div class="mb-3">
                            <label>Description</label>
                            <textarea placeholder="Enter Accomodation Type Description"></textarea>
                        </div>
                    </form>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary">Save changes</button>
                </div>
            </div>

        }
    </div>
</div>

<script>
    $("createButton").click(function () {
        $.ajax({
            url: 'Url.Action("Action", " AccomodationTypes")'
            /*,*/
           /*  method: 'post',*/
          /*data: $("#").serialize()*/

        })
            .done(function (response) {
                $('.modal-dialog', '#actionModal').html(response);
              /*  $("#actionModal .modal-dialog").html(response);*/
            });
    });
</script>
/*< !--Modal -->*/
    <div class="modal fade" id="exampleModal" tabindex="-1" >
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    ...
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                    <button type="button" class="btn btn-primary">Save changes</button>
                </div>
            </div>
        </div>
    </div>



@*@model HMS.Areas.Dashboard.ViewModels.AccomodationTypesListingModel
<div class="row">
    <div class="col col-lg-12">
        <h2>Accomodation Types</h2>
        <hr />
    </div>
</div>

<div class="row mb-3">
    <div class="col col-lg-12">
        <form>
            <div class="input-group">
                <input type="text" name="searchterm" class="form-control" placeholder="Search." value="@*Model.SearchTerm*" required>
                @*<div class="input-group-append">
                    <button class="btn btn-outline-primary" type="submit"><i class="fas fa-search mr-1"></i> Search</button>
                    <a class="btn btn-outline-secondary" href="Url.Action("Index", "AccomodationTypes")"><i class="fas fa-redo-alt mr-1"></i> Reset</a>
                    <button data-href="Url.Action("Action", "AccomodationTypes")" class="data-btn btn btn-outline-success" type="button" data-toggle="modal" data-target="#actionModal"><i class="fas fa-plus mr-1"></i> Create</button>
                </div>
            </div>
        </form>
    </div>
</div>

<div class="row">
    <div class="col col-lg-12">
        @{
           
        }
    </div>
</div>

<div class="modal fade" id="actionModal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
    </div>
</div>

<script>
    $(".data-btn").click(function () {
        $.ajax({
            url: $(this).attr("data-href"),
        })
            .done(function (response) {
                $("#actionModal .modal-dialog").html(response);
            });
    });
</script>*@*